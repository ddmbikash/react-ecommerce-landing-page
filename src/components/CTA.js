import React from 'react';

const CTA = () => {
  return (
    <section className="cta">
      <h2>Ready to Shop?</h2>
      <button>Start Shopping</button>
    </section>
  );
};

export default CTA;
